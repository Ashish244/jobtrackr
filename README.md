# JobTrackr

JobTrackr is a Spring Boot REST API that helps users track their job applications — from submission to interview and offer — all in one place.

## 🚀 Features
- CRUD operations for job applications
- Swagger UI for testing APIs
- H2 in-memory database (switchable to PostgreSQL)
- Built with Java 17, Spring Boot 3.3.4, Maven

## 🛠️ Tech Stack
**Backend:** Java 17, Spring Boot, JPA, H2  
**Build Tool:** Maven  
**API Docs:** Swagger (OpenAPI 3.0)

## ⚙️ Run Locally
1. Clone the repo
   ```bash
   git clone https://github.com/Ashish244/jobtrackr.git
   cd jobtrackr
